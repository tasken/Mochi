# Copy to .env and fill in your LAN address. Used by ./server.py for flag
# defaults; explicit CLI flags always win. Without a .env, the server binds
# 127.0.0.1:8443 over plain HTTP.
MOCHI_BIND=YOUR_LAN_IP
MOCHI_HOST=YOUR_LAN_IP
MOCHI_PORT=8443
MOCHI_CERT=.cert/cert.pem
MOCHI_KEY=.cert/key.pem
